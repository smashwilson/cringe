#!/usr/bin/env node

const spawn = require("child_process").spawn;
const util = require("util");

console.log("Injected fake docker!");
console.log(`Command line args: ${util.inspect(process.argv)}`);
